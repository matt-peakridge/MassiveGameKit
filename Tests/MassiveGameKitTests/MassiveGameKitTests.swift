    import XCTest
    @testable import MassiveGameKit

    final class MassiveGameKitTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(MassiveGameKit().text, "Hello, World!")
        }
    }
